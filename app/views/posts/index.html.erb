<% @posts.each do |post| %>
  <div class="white">
      <%= image_tag post.image.url(:medium) %>
      <br>
      <%= post.description %>
    </div>
   <%= link_to "Delete", post, :method => :delete, :confirm => "Are you sure?" %>
  <% end %>
